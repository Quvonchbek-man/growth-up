# Faqat ishlab chiqish va testlar uchun. Serverda kerak emas:
#   pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

pytest>=9.0
pytest-asyncio>=1.0
httpx>=0.28          # API testlari ASGI orqali, tarmoqqa chiqmasdan
